# User window and workspace rule overrides for Hyprland
{
  windowRules = {
    add = [
      # Example: Add custom window rules
      # "opacity 0.9, class:^(discord)$"
    ];
  };

  workspaceRules = {
    add = [
      # Example: Add custom workspace rules
    ];
  };
}
