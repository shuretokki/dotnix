{
  # Public API for module exports (to be populated in Phase 2)
  nixosModules = {
    core = { };
    display = { };
    profiles = { };
  };

  homeModules = {
    apps = { };
    cli = { };
  };
}
